PGHOST=localhost
PGPORT=5432
PGDATABASE=orthoconnect
# En macOS/Homebrew suele bastar su usuario del sistema si PGUSER no está definido.
PGUSER=
PGPASSWORD=

# Los scripts schema/seed los aporta el equipo de BD (no están en este repo).
# Ver sql/README.md para el contrato con la aplicación.
#
# Desde la raíz del proyecto:
#   PYTHONPATH=. python run.py
